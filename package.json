{
	"name": "od-update-history",
	"version": "0.2.0",
	"private": true,
	"description": "Development environment for the OD Update History WordPress plugin.",
	"scripts": {
		"env:start": "wp-env start",
		"env:stop": "wp-env stop",
		"env:status": "wp-env status",
		"env:logs": "wp-env logs",
		"env:cli": "wp-env run cli wp",
		"test": "npm run test:php",
		"test:php": "wp-env start && wp-env run tests-cli --env-cwd=wp-content/plugins/od-update-history vendor/bin/phpunit -c phpunit.xml.dist"
	},
	"devDependencies": {
		"@wordpress/env": "^11.11.0"
	}
}
